[workspace]

members = [
    "bot",
    "cms",
    "db",
    "frontend",
    "models",
]
exclude = ["cli"]

[workspace.dependencies]
sqlx = { version = "0.6", features = [ "runtime-tokio-native-tls" , "sqlite", "migrate", "offline" ] }
dotenv = "0.15"